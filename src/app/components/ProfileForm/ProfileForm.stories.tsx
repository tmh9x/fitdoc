import React from 'react';
import ProfileForm from './ProfileForm';

export default {
  title: 'Components/ProfileForm',
  component: ProfileForm,
};

export const Inputfield = (): JSX.Element => <ProfileForm />;
