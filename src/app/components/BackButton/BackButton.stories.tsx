import React from 'react';
import BackButton from './BackButton';

export default {
  title: 'Components/BackButton',
  component: BackButton,
};

export const Card = (): JSX.Element => <BackButton thumbnail="back.png" />;
